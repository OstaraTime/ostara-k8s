apiVersion: v1
kind: Service
metadata:
  name: ostara-api-kiosk
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: ostara-api-kiosk
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
