apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ostara-qr-generator-ingress
  namespace: ostara
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  ingressClassName: traefik
  rules:
    - host: ostara-rso.031268394.xyz
      http:
        paths:
          - path: /generate-pdf
            pathType: Prefix
            backend:
              service:
                name: ostara-qr-generator
                port:
                  number: 80
